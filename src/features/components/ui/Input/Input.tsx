import './Input.scss';

export const Input = () => {
    return (
        <input
            className="input"
            type="text"
            placeholder="What needs to be done?"
        />
    );
};
