'use client';

import './Button.scss';

export const ButtonAddTask = () => {
    return (
        <div className="createBtn">
            <button className="buttonAdd">Создать задачу</button>
        </div>
    );
};
